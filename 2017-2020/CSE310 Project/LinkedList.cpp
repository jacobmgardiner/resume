#include "LinkedList.h"

//template<typename T>
//LinkedList<T>::LinkedList() = default;
//
//template <typename T> void LinkedList<T>::insertHead(T data) {
////    if (root == nullptr) root = node; return;
//    Node<T> tmp = root;
////    root = Node(data);
//    root.data = data;
//    root.next = tmp;
//}
//
//template <typename T>
//bool LinkedList<T>::deleteNode(T data) {
//    Node<T> last;
//    Node<T> n = root;
//    while (n != nullptr) {
//        if (n.data == data) {
//            if (last != nullptr) {
//                last.next = n.next;
//                delete n;
//                return true;
//            }
//        }
//        last = n;
//        n = n.next;
//    }
//    return false;
//}
